<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myharness.codex.mapper.SysUserMapper">

    <resultMap id="sysUserResultMap" type="com.myharness.codex.entity.po.SysUserPO">
        <id property="id" column="id"/>
        <result property="tokenVersion" column="token_version"/>
        <result property="mustChangePassword" column="must_change_password"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="displayName" column="display_name"/>
        <result property="status" column="status"/>
        <result property="emailVerifiedAt" column="email_verified_at"/>
        <result property="activatedAt" column="activated_at"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByEmail" resultMap="sysUserResultMap">
        SELECT id, email, password_hash, display_name, status, email_verified_at, activated_at, token_version, must_change_password, last_login_at, created_at, updated_at
        FROM sys_user
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="selectById" resultMap="sysUserResultMap">
        SELECT id, email, password_hash, display_name, status, email_verified_at, activated_at, token_version, must_change_password, last_login_at, created_at, updated_at
        FROM sys_user
        WHERE id = #{id}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.myharness.codex.entity.po.SysUserPO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (email, password_hash, display_name, status, must_change_password, email_verified_at, activated_at)
        VALUES (#{email}, #{passwordHash}, #{displayName}, #{status}, #{mustChangePassword}, #{emailVerifiedAt}, #{activatedAt})
    </insert>

    <update id="updateLastLoginAt">
        UPDATE sys_user
        SET last_login_at = #{lastLoginAt}
        WHERE id = #{id}
    </update>
</mapper>
